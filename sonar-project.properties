# Required meta data
sonar.projectKey=project-spring
sonar.projectName=project-spring
sonar.projectVersion=1.0

# Language
sonar.language=java
sonar.java.source=1.8

# Comma-separated paths to directories with sources (required)
sonar.sources=src/main/java,src/test/java

# path to test source directories (optional)
#sonar.tests=src/test/java

# path to modelProject binaries (optional), for example directory of Java bytecode
sonar.java.binaries=build/classes/java/main,build/classes/java/test

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# =====================================================
#   Meta-data for the modelProject
# =====================================================

sonar.links.homepage=https://github.com/MinionsGroup/spring-pivotal
sonar.links.ci=https://travis-ci.org/MinionsGroup/spring-pivotal
sonar.links.scm=https://github.com/MinionsGroup/spring-pivotal
sonar.links.issue=https://github.com/MinionsGroup/spring-pivotal/issues
